<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Command O Conference" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-black text-white dark:bg-black dark:text-white transition-colors duration-300">
    <!-- Navigation Header -->
    <nav class="bg-gray-900 border-b border-gray-800 px-4 py-3">
      <div class="max-w-7xl mx-auto">
        <!-- Navigation -->
        <div class="flex items-center justify-between">
          <!-- Logo -->
          <div class="flex items-center space-x-4">
            <%= image_tag "omise-logo.png", alt: "Omise", class: "h-8 w-auto" %>
            <span class="text-xl font-bold text-omise-teal">Command O</span>
          </div>
          
          <!-- Profile Section with Dropdown -->
          <div class="relative flex justify-end" data-controller="mobile-dropdown">
            <button 
              class="flex items-center space-x-2 p-1 rounded-lg hover:bg-gray-800 transition-colors"
              data-action="click->mobile-dropdown#toggle"
            >
              <!-- Welcome Text -->
              <div class="hidden sm:block">
                <span class="text-sm text-gray-300">Welcome, <%= current_user&.name&.split&.first || "Guest" %></span>
              </div>
              
              <!-- Profile Circle with Photo or Initials -->
              <div class="w-8 h-8 rounded-full overflow-hidden flex items-center justify-center">
                <% if current_user&.has_face_photo? %>
                  <%= image_tag polymorphic_path(current_user.face_photo, only_path: true), 
                        alt: current_user.name, 
                        class: "profile-photo" %>
                <% else %>
                  <div class="profile-photo-fallback bg-omise-teal">
                    <span class="text-xs font-bold">
                      <%= current_user&.name&.split&.map(&:first)&.join&.upcase || "G" %>
                    </span>
                  </div>
                <% end %>
              </div>
              
              <!-- Dropdown Arrow -->
              <svg class="w-4 h-4 text-gray-400 transition-transform duration-200" data-mobile-dropdown-target="arrow" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </button>
            
            <!-- Dropdown Menu (Hidden by default) -->
            <div 
              class="mobile-dropdown-menu absolute right-0 top-full mt-3 w-48 bg-gray-800 rounded-lg shadow-xl border border-gray-700 hidden z-50 max-w-[calc(100vw-2rem)]"
              data-mobile-dropdown-target="menu"
              style="right: 0; left: auto; transform: translateX(0); min-width: 200px;"
            >
              <div class="py-1">
                <!-- User Info -->
                <div class="px-4 py-3 border-b border-gray-700">
                  <div class="flex items-center space-x-3">
                    <!-- User Photo or Initials -->
                    <div class="w-10 h-10 rounded-full overflow-hidden flex items-center justify-center flex-shrink-0">
                      <% if current_user&.has_face_photo? %>
                        <%= image_tag polymorphic_path(current_user.face_photo, only_path: true), 
                              alt: current_user.name, 
                              class: "profile-photo" %>
                      <% else %>
                        <div class="profile-photo-fallback bg-omise-teal">
                          <span class="text-sm font-bold">
                            <%= current_user&.name&.split&.map(&:first)&.join&.upcase || "G" %>
                          </span>
                        </div>
                      <% end %>
                    </div>
                    
                    <!-- User Details -->
                    <div class="min-w-0 flex-1">
                      <div class="text-sm font-medium text-white truncate">
                        <%= current_user&.name || "Guest" %>
                      </div>
                      <div class="text-xs text-gray-400 truncate">
                        <%= current_user&.email %>
                      </div>
                    </div>
                  </div>
                </div>
                
                <!-- Menu Items -->
                <div class="py-1">
                  <!-- Admin Link (only visible to admins) -->
                  <% if current_user&.admin? %>
                    <%= link_to admin_dashboard_path, 
                          class: "flex items-center px-4 py-2 text-sm text-gray-300 hover:bg-gray-700 hover:text-white transition-colors" do %>
                      <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                      </svg>
                      Admin Dashboard
                    <% end %>
                  <% end %>
                  
                  
                  <!-- Logout -->
                  <%= link_to logout_path, 
                        data: { "turbo-method": :delete },
                        class: "flex items-center px-4 py-2 text-sm text-red-400 hover:bg-gray-700 hover:text-red-300 transition-colors" do %>
                    <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                    </svg>
                    Logout
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <main class="min-h-screen">
      <%= yield %>
    </main>
  </body>
</html>
